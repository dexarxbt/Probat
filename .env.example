# Optional local API settings
PROBAT_LOG_LEVEL=info
NODE_ENV=development

# Optional deterministic demo target port (loopback only)
DEMO_TARGET_PORT=4321

# Optional Kane executable override. Probat does not automatically load this file;
# set the variable in your shell when needed.
# KANE_CLI_PATH=C:\\path\\to\\kane-cli.exe
